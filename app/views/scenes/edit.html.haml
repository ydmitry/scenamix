-# edit.html.haml using partial

%h1 Edit Existing Scene

= form_for @scene do |f|

  = f.label :title
  = f.text_field :title

  = f.label :description
  = f.text_area :description

  = f.submit 'Update scene Info'
